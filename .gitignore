node_modules
dist/
# lib-dist/ is generated but COMMITTED — other repos vendor it, and a fresh
# clone must be able to hand them the file without a build step first. It is
# NOT matched by dist/ above, which only matches a directory named exactly dist.
*.tsbuildinfo
.DS_Store
.claude/worktrees/
tsconfig.tsbuildinfo
